import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
  textInput: {
    width: '100%',
    maxWidth: 500,
    height: 60,
    borderWidth: 1,
    borderColor: "#ececec",
    backgroundColor: "#ececec",
    borderRadius: 100,
    padding: 20,
    fontSize: 14,
  },
});
